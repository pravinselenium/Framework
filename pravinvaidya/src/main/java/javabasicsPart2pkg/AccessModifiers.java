package javabasicsPart2pkg;

public class AccessModifiers {
	
	

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		//public , private, protected, default
		//Default - access method anywhere only in package (if nothing is given it means default
		//Public - variables/method as public then u will have access across the packages
		//Private - if u mark variable/method as private then u can access it outside the class of same package as well
		//Protected - if variable /method is protected then u can access in subclasses(access by chind classes when extends to parent class who has protected v/m only (other packages)

	}

}
